import 'package:flutter/material.dart';

class AppColors {
  // Green-based dark theme palette
  static const Color primaryColor = Color(0xFF2E7D32);        // Green base
  static const Color primaryLightColor = Color(0xFF66BB6A);   // Light green
  static const Color secondaryColor = Color(0xFF00796B);      // Teal base
  
  static const Color backgroundColor = Color(0xFF121212);     // Dark background
  static const Color cardColor = Color(0xFF1E1E1E);          // Slightly lighter than background
  static const Color surfaceColor = Color(0xFF242424);        // Surface color for cards, etc.
  
  static const Color textPrimaryColor = Colors.white;    // White text
  static const Color textSecondaryColor = Color(0xFFB0B0B0);  // Light grey text
  
  static const Color errorColor = Color(0xFFEF5350);          // Error red
  static const Color dividerColor = Color(0xFF2A2A2A);        // Divider grey
  
  // Additional colors for playful UI
  static const Color successColor = Color(0xFF4CAF50);        // Success green
  static const Color warningColor = Color(0xFFFFB74D);        // Warning amber
  
  // Accent colors for UI interest
  static const Color accentPurple = Color(0xFF7B1FA2);        // Accent purple
  static const Color accentPink = Color(0xFFD81B60);          // Accent pink
  static const Color accentYellow = Color(0xFFFFC107);        // Accent yellow
  
  // New vibrant accent colors
  static const Color accentTeal = Color(0xFF00BCD4);    // Vibrant teal
  static const Color accentOrange = Color(0xFFFF5722);  // Vibrant orange
  static const Color accentMagenta = Color(0xFFE040FB); // Vibrant magenta
  static const Color accentLime = Color(0xFFCDDC39);    // Lime green for contrast
  static const Color accentCyan = Color(0xFF00E5FF);    // Bright cyan
  
  // Light surface color
  static const Color lightSurface = Color(0xFF2C2C2C);        // Lighter surface for contrast
} 