import 'package:flutter/material.dart';

class AppColors {
  // Green-based dark theme palette
  static const Color primaryColor = Color(0xFF00A651);        // Main green
  static const Color primaryLightColor = Color(0xFF4AD66D);   // Light green
  static const Color secondaryColor = Color(0xFF32CDD9);      // Teal accent
  
  static const Color backgroundColor = Color(0xFF121212);     // Dark background
  static const Color cardColor = Color(0xFF1E1E1E);          // Slightly lighter than background
  static const Color surfaceColor = Color(0xFF282828);        // Surface color for cards, etc.
  
  static const Color textPrimaryColor = Color(0xFFFFFFFF);    // White text
  static const Color textSecondaryColor = Color(0xFFBBBBBB);  // Light grey text
  
  static const Color errorColor = Color(0xFFFF5252);          // Error red
  static const Color dividerColor = Color(0xFF323232);        // Divider grey
  
  // Additional colors for playful UI
  static const Color successColor = Color(0xFF4CAF50);        // Success green
  static const Color warningColor = Color(0xFFFFC107);        // Warning amber
  static const Color accentPurple = Color(0xFF9C27B0);        // Accent purple
  static const Color accentPink = Color(0xFFFF4081);          // Accent pink
  static const Color accentYellow = Color(0xFFFFEB3B);        // Accent yellow
  static const Color lightSurface = Color(0xFF303030);        // Lighter surface for contrast
} 